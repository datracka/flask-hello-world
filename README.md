# Hello word app

intent of having

- Flask app
- GraphQL Endpoint
- React ecosystem (babel, webpack and so on)
- deployable in Heroku out of the box

launch app locally:

- pipenv run python app.py

